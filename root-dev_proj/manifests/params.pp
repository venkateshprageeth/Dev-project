class root-dev_proj::params {
        $pack = ['docker','xinetd','wget']
        $ser  = ['docker']
        $ser_status = 'running'
	$support_file_Dockerfile = '/root/Dockerfile'
	$support_file_index = '/root/index.html'
}
